let num = ('请输入一个数字:')
// 判断数字是否小于10
num = num < 10 ? 0 + num : num
// 字符串加上任何一个数字都是字符串，是直接拼接的;


switch (数据) {
    case 值1:
        代码1
        break
    case 值2:
        代码2
        break
    default:
        代码n
        break
}